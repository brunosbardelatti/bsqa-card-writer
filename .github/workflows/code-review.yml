name: AI Code Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  ai-review:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}

    steps:
      - name: 🔄 Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🔍 Gerando Diff
        run: |
          echo "📊 Gerando diff do PR..."
          git fetch origin ${{ github.base_ref }}
          git diff origin/${{ github.base_ref }}...HEAD > diff.txt

          if [ ! -f diff.txt ] || [ ! -s diff.txt ]; then
            echo "⚠️ Diff vazio - criando diff padrão"
            echo "No changes to review" > diff.txt
          fi

          DIFF_SIZE=$(wc -c < diff.txt)
          echo "📏 Tamanho do diff: $DIFF_SIZE bytes"
          
          if [ $DIFF_SIZE -gt 51200 ]; then
            echo "✂️ Truncando diff (muito grande)..."
            head -c 51200 diff.txt > diff_truncated.txt
            echo -e "\n\n... [DIFF TRUNCADO - MUITAS ALTERAÇÕES] ..." >> diff_truncated.txt
            mv diff_truncated.txt diff.txt
          fi

      - name: 📝 Baixando Prompt do Google Drive
        run: |
          echo "📥 Baixando prompt template do Google Drive..."
          
          # URL de download direto do Google Drive usando FILE_ID
          GDRIVE_URL="https://drive.google.com/uc?export=download&id=1jD7xwCOg6l4O0Y3qKpa9-A7V7ExeeK_V"
          
          if curl -s --fail --max-time 30 "$GDRIVE_URL" -o prompt_template.txt; then
            if [ -s prompt_template.txt ]; then
              echo "✅ Prompt baixado com sucesso do Google Drive"
              echo "📊 Tamanho: $(wc -c < prompt_template.txt) bytes"
              echo "📋 Preview do prompt:"
              head -n 5 prompt_template.txt
            else
              echo "❌ Arquivo baixado está vazio"
              exit 1
            fi
          else
            echo "❌ Falha ao baixar prompt do Google Drive"
            exit 1
          fi

      - name: 🛠️ Preparando Prompt Final
        run: |
          echo "🔧 Montando prompt final com diff..."
          
          # Ler template e diff
          TEMPLATE_CONTENT=$(cat prompt_template.txt)
          DIFF_CONTENT=$(cat diff.txt)
          
          # Substituir {requirements} pelo diff de forma simples
          echo "$TEMPLATE_CONTENT" | sed "s/{requirements}/$(printf '%s\n' "$DIFF_CONTENT" | sed 's/[[\.*^$()+?{|]/\\&/g')/g" > prompt.txt
          
          echo "✅ Prompt final preparado"
          echo "📊 Tamanho final: $(wc -c < prompt.txt) bytes"

      - name: 🔐 Autenticação StackSpot
        run: |
          echo "🔐 Obtendo token StackSpot..."
          
          TOKEN_RESPONSE=$(curl -s --fail --max-time 30 \
            --location 'https://idm.stackspot.com/stackspot-freemium/oidc/oauth/token' \
            --header 'Content-Type: application/x-www-form-urlencoded' \
            --data-urlencode "client_id=${{ secrets.STACKSPOT_CLIENT_ID }}" \
            --data-urlencode 'grant_type=client_credentials' \
            --data-urlencode "client_secret=${{ secrets.STACKSPOT_CLIENT_SECRET }}") || {
            echo "❌ Falha na autenticação StackSpot"
            exit 1
          }
          
          if ! echo "$TOKEN_RESPONSE" | jq . > /dev/null 2>&1; then
            echo "❌ Resposta de autenticação inválida"
            exit 1
          fi
          
          if echo "$TOKEN_RESPONSE" | jq -e '.error' > /dev/null 2>&1; then
            ERROR=$(echo "$TOKEN_RESPONSE" | jq -r '.error_description // .error')
            echo "❌ Erro de autenticação: $ERROR"
            exit 1
          fi
          
          ACCESS_TOKEN=$(echo "$TOKEN_RESPONSE" | jq -r '.access_token')
          if [ -z "$ACCESS_TOKEN" ] || [ "$ACCESS_TOKEN" = "null" ]; then
            echo "❌ Token não encontrado na resposta"
            exit 1
          fi
          
          echo "::add-mask::$ACCESS_TOKEN"
          echo "ACCESS_TOKEN=$ACCESS_TOKEN" >> $GITHUB_ENV
          echo "✅ Autenticação bem-sucedida"

      - name: 🤖 Análise com StackSpot AI
        run: |
          echo "🚀 Enviando para análise da IA..."
          
          PROMPT_CONTENT=$(cat prompt.txt)
          ESCAPED_PROMPT=$(printf '%s' "$PROMPT_CONTENT" | jq -Rs .)
          
          RESPONSE=$(curl -s -w "\n###STATUS###%{http_code}" \
            --max-time 60 \
            --location "https://genai-inference-app.stackspot.com/v1/agent/${{ secrets.STACKSPOT_AGENT_ID }}/chat" \
            --header 'Content-Type: application/json' \
            --header "Authorization: Bearer $ACCESS_TOKEN" \
            --data "{
              \"streaming\": false,
              \"user_prompt\": $ESCAPED_PROMPT,
              \"stackspot_knowledge\": false,
              \"return_ks_in_response\": false
            }")
          
          HTTP_STATUS=$(echo "$RESPONSE" | tail -n 1 | sed 's/.*###STATUS###//')
          RESPONSE_BODY=$(echo "$RESPONSE" | sed '$d')
          
          if [ "$HTTP_STATUS" != "200" ]; then
            echo "❌ Erro na API StackSpot (HTTP $HTTP_STATUS)"
            exit 1
          fi
          
          if ! echo "$RESPONSE_BODY" | jq . > /dev/null 2>&1; then
            echo "❌ Resposta da IA inválida"
            exit 1
          fi
          
          REVIEW_OUTPUT=$(echo "$RESPONSE_BODY" | jq -r '.message // .output_text // empty')
          
          if [ -z "$REVIEW_OUTPUT" ] || [ "$REVIEW_OUTPUT" = "null" ]; then
            echo "❌ Falha ao extrair resposta da IA"
            exit 1
          fi
          
          echo "$REVIEW_OUTPUT" > review.txt
          echo "✅ Análise concluída"

      - name: 💬 Comentário no PR
        run: |
          echo "💬 Adicionando comentário ao PR..."
          
          {
            echo "## 🤖 Revisão Automática de Código - StackSpot AI"
            echo ""
            echo "**PR:** #${{ github.event.pull_request.number }}"
            echo "**Branch:** \`${{ github.head_ref }}\` → \`${{ github.base_ref }}\`"
            echo "**Prompt Source:** Google Drive"
            echo ""
            echo "---"
            echo ""
            cat review.txt
            echo ""
            echo "---"
            echo "*Gerado em $(date -u '+%Y-%m-%d %H:%M:%S UTC')*"
          } > final_review.txt
          
          gh pr comment ${{ github.event.pull_request.number }} --body-file final_review.txt
          echo "✅ Comentário adicionado com sucesso"

      - name: ⚡ Validação do Resultado
        run: |
          echo "🔍 Validando resultado..."
          
          if grep -q "REPROVADO" review.txt; then
            echo "❌ REPROVADO - Problemas críticos encontrados"
            exit 1
          elif grep -q "APROVADO" review.txt; then
            echo "✅ APROVADO - Código pode prosseguir"
          else
            echo "⚠️ INDETERMINADO - Assumindo aprovação"
          fi

      - name: 🧹 Limpeza
        if: always()
        run: |
          rm -f diff.txt prompt_template.txt prompt.txt review.txt final_review.txt
          echo "✅ Arquivos temporários removidos"