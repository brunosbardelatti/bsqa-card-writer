name: AI Code Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  ai-review:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}

    steps:
      - name: 🔄 Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 📦 Instalando dependências necessárias
        run: |
          echo "📦 Instalando dependências necessárias..."
          if ! command -v jq &> /dev/null; then
            echo "Instalando jq..."
            sudo apt update
            sudo apt install -y jq
          else
            echo "✅ jq já está instalado"
          fi

          if ! command -v gh &> /dev/null; then
            echo "Instalando GitHub CLI..."
            curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | sudo dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg
            echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null
            sudo apt update
            sudo apt install -y gh
          else
            echo "✅ GitHub CLI já está instalado"
          fi
          echo "✅ Todas as dependências foram verificadas"

      - name: 🔍 Gerando Diff
        run: |
          echo "Fetching base branch..."
          git fetch origin ${{ github.base_ref }}
          echo "Generating diff..."
          git diff origin/${{ github.base_ref }}...HEAD > diff.txt

          if [ ! -f diff.txt ] || [ ! -s diff.txt ]; then
            echo "❌ Erro: Não foi possível gerar o diff ou o diff está vazio"
            echo "No changes to review" > diff.txt
          fi

          DIFF_SIZE=$(wc -c < diff.txt)
          if [ $DIFF_SIZE -gt 51200 ]; then
            echo "⚠️ Diff muito grande ($DIFF_SIZE bytes), truncando..."
            head -c 51200 diff.txt > diff_truncated.txt
            echo -e "\n\n... [DIFF TRUNCADO - MUITAS ALTERAÇÕES] ..." >> diff_truncated.txt
            mv diff_truncated.txt diff.txt
          fi

          echo "=== DIFF FILE PREVIEW ==="
          head -n 20 diff.txt

      - name: 🔐 Gerando Token StackSpot
        run: |
          echo "Gerando token StackSpot..."
          TOKEN_RESPONSE=$(curl -s --location 'https://idm.stackspot.com/stackspot-freemium/oidc/oauth/token'             --header 'Content-Type: application/x-www-form-urlencoded'             --data-urlencode "client_id=${{ secrets.STACKSPOT_CLIENT_ID }}"             --data-urlencode 'grant_type=client_credentials'             --data-urlencode "client_secret=${{ secrets.STACKSPOT_CLIENT_SECRET }}")

          if echo "$TOKEN_RESPONSE" | jq -e '.access_token' > /dev/null 2>&1; then
            ACCESS_TOKEN=$(echo "$TOKEN_RESPONSE" | jq -r '.access_token')
            echo "ACCESS_TOKEN=$ACCESS_TOKEN" >> $GITHUB_ENV
            echo "✅ Autenticação bem-sucedida"
          else
            echo "❌ Erro: Token de acesso não encontrado na resposta"
            echo "Response: $TOKEN_RESPONSE"
            exit 1
          fi

      - name: 🤖 Chamando StackSpot AI para revisão
        run: |
          echo "🤖 Chamando StackSpot AI para revisão..."

          {
            echo "You are a senior code reviewer."
            echo
            echo "You will receive a raw Git diff via {requirements}."
            echo "This diff compares a feature branch to the homolog branch."
            echo
            echo "**Your task:**"
            echo
            echo "1. Analyze the diff and automatically detect the programming language or framework (e.g., Python, Robot Framework, Node.js, PHP)."
            echo "   - Apply **best practices, linting rules, and conventions** specific to the detected technology."
            echo
            echo "2. Provide a **code review in Portuguese (Brazil)** with the following structure:"
            echo "   - **Feedback por Arquivo**: List only the files where there are **problems, issues, or relevant improvement points**."
            echo "     - Show **only the problematic snippets** using code blocks (\`\`\`), explaining **what is wrong or can be improved**."
            echo "     - Provide **objective correction/refactoring suggestions**."
            echo "     - **Explain the practical impact of each issue or suggestion** (e.g., \"isso pode causar falhas intermitentes nos testes\")."
            echo "     - Include **Opiniões** only when they are optional adjustments that improve clarity, performance, or standardization."
            echo
            echo "3. **Classificação de Achados**"
            echo "   - **Críticos** – Bugs, logical errors, or security vulnerabilities."
            echo "   - **Melhorias** – Refactorings, best practices, performance, linting."
            echo "   - **Opiniões** – Optional style or clarity improvements."
            echo
            echo "4. **Resumo Final**"
            echo "   - Return **APROVADO** if no critical issues are found."
            echo "   - Return **REPROVADO** if critical issues are detected."
            echo "   - List only the relevant action points."
            echo
            echo "---"
            echo
            echo "**Important Rules:**"
            echo "- Do not describe correct or trivial changes."
            echo "- Provide only **useful technical feedback** (critical issues or real improvements)."
            echo "- Always explain the practical impact of problems or improvements."
            echo "- Do not include the technology detection section in the response."
            echo "- Use markdown (\`\`\`) to show code snippets when necessary."
            echo "- If there are no issues, finish with a brief positive summary and **APROVADO**."
            echo "- If there are problems, finish with a brief summary and **REPROVADO**."
            echo
            echo "<DIFF_START>"
            cat diff.txt
            echo "<DIFF_END>"
          } > prompt.txt

          PROMPT_CONTENT=$(cat prompt.txt)

          echo "=== PROMPT SENDIDO (primeiras 40 linhas) ==="
          head -n 40 prompt.txt

          echo "=== ENVIANDO REQUISIÇÃO PARA STACKSPOT ==="
          RESPONSE=$(curl -s -w "\n###STATUS###%{http_code}" --location             'https://genai-inference-app.stackspot.com/v1/agent/01JZ4135ZCD42NN04PRGA4XA1C/chat'             --header 'Content-Type: application/json'             --header "Authorization: Bearer $ACCESS_TOKEN"             --data "$(jq -n --arg prompt "$PROMPT_CONTENT" '{
              "streaming": false,
              "user_prompt": $prompt,
              "stackspot_knowledge": false,
              "return_ks_in_response": false
            }')")

          HTTP_STATUS=$(echo "$RESPONSE" | grep "###STATUS###" | sed 's/###STATUS###//')
          RESPONSE_BODY=$(echo "$RESPONSE" | sed '/###STATUS###/d')

          echo "=== HTTP STATUS: $HTTP_STATUS ==="
          echo "=== RESPOSTA COMPLETA (RAW) ==="
          echo "$RESPONSE_BODY"

          if echo "$RESPONSE_BODY" | jq -e '.output_text' > /dev/null 2>&1; then
            echo "$RESPONSE_BODY" | jq -r '.output_text' > review.txt
          elif echo "$RESPONSE_BODY" | jq -e '.message' > /dev/null 2>&1; then
            echo "$RESPONSE_BODY" | jq -r '.message' > review.txt
          else
            echo "⚠️ Falha na análise de código por IA - verifique manualmente" > review.txt
          fi

          echo "=== AI REVIEW OUTPUT ==="
          cat review.txt

      - name: 💬 Adicionando comentário ao PR
        run: |
          echo "💬 Adicionando comentário ao PR..."
          echo "## 🤖 Revisão Automática de Código - StackSpot AI" > final_review.txt
          echo "" >> final_review.txt
          echo "**PR:** #${{ github.event.pull_request.number }}" >> final_review.txt
          echo "**Branch:** \`${{ github.head_ref }}\` → \`${{ github.base_ref }}\`" >> final_review.txt
          echo "**Commit:** ${{ github.sha }}" >> final_review.txt
          echo "" >> final_review.txt
          echo "---" >> final_review.txt
          echo "" >> final_review.txt
          cat review.txt >> final_review.txt
          echo "" >> final_review.txt
          echo "---" >> final_review.txt
          echo "*Revisão gerada automaticamente em $(date -u '+%Y-%m-%d %H:%M:%S UTC')*" >> final_review.txt

          gh pr comment ${{ github.event.pull_request.number }} --body-file final_review.txt || {
            echo "❌ Erro ao adicionar comentário"
            cat final_review.txt
            exit 1
          }

      - name: 🧹 Limpeza
        run: |
          rm -f diff.txt prompt.txt review.txt final_review.txt
          echo "🧹 Limpeza concluída"
